import 'package:flutter/material.dart';

class BookMap extends StatelessWidget {
  const BookMap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('북맵'),
    );
  }
}